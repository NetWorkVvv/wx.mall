<view class="tabs_s">
  <view class="tabs_title">
    <view class="tabs_title_item {{item.isActive?'active':''}}" data-index="{{index}}" bindtap="handleName"
      wx:for="{{propTabs}}" wx:key="id">
      {{item.name}}
    </view>

  </view>
  <view class="tabs_content">
    <slot></slot>
  </view>
</view>